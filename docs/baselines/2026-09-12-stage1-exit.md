# 阶段 1 出口核销(2026-09-12)

对照 `docs/roadmap.md` 阶段 1 逐项核销;证据 = 仓内冻结基线(双 run 目录 +
`2026-09-12-full149-dual-checkpoint-summary.md` + `eval/manifests/golden-freeze.json`)。

## 出口条件逐项

| 条件 | 状态 | 证据 |
|---|---|---|
| pilot-ramp 定容(每类 10 条 pilot → 实测 judge 方差 → 按效应量定容) | ✅(#8) | `docs/baselines/2026-08-31-pilot30-sizing-decision.md`:50/类 → golden 149 条 |
| 双公开 checkpoint 基线(mBERT-base + XLM-R-large,零训练成本) | ✅ | `runs/20260912T130801…-full149-mbert-base-*`、`runs/20260912T150615…-full149-xlmr-large-*`(四指标 × 扫档 0.1–0.5 × 149 条) |
| 压缩延迟档案(p95,按 checkpoint 按档) | ✅ | 同上 run 目录逐档 latency 块;mBERT p95 2.9–6.4ms;XLM-R p95 17.8–25.8s |
| 后手牌激活判据(质量显著更高 **且** 延迟可接受 **且** 接口相容) | ✅ 判定 = **不激活** | 质量差仅 0.4 档 CI 下界 > 0(1/5);延迟 ≈ 4000×;`.roberta` 非 drop-in(decisions §6) |
| 冻结纪律(golden 冻结:内容哈希 + 仓库 commit 进报告,可审计) | ✅ | 每份报告 harness.git_commit + golden.sha256;`eval/manifests/golden-freeze.json`(sha256 `fb728d6a5042…`) |
| 判据口径(paired bootstrap 按 prompt 重采样,95% CI,下界不为负) | ✅ | 每档任务级质量 delta + CI + 判据字段进报告 |
| 版本钉死(judge / 下游作答 / rubric 进报告) | ✅ | 报告 task_quality 段:answer/judge 型号、rubric 版本 + sha256、served 模型 |
| MeetingBank 排除声明与语料许可链 | ✅ | golden 校验器硬拒;judge 切换与 GLM Coding Plan 端点口径注记见 `docs/evaluation.md`;逐条许可溯源在 golden source 字段与 `eval/manifests/` |

## 结论

阶段 1(评测基线 = 验收尺)**完成**。v1 走 mBERT-base 基底;后手牌
(XLM-R-large)判据未满足,不激活。后续:阶段 2 数据管线不受阻
(其大规模 teacher 花费门 = 基线数字已产出,可启动);阶段 3 微调的
验收对比基线 = 本冻结基线。

## 运行履历(审计)

- judge 钉子变迁:kimi/kimi-k2(上游故障中止)→ owner 授权切
  zhipu/glm-5.1(coding-plan 端点);全程两 checkpoint 同 judge。
- 中断与恢复:网关 502/503 停摆、kimi 故障、网关进程重启、内容审查重答
  ——全部由 crash journal(内容寻址重放)吸收,零重复计费;最终补齐为
  mBERT 208 条 + XLM 151 条增量。
- 延迟口径:XLM-R 延迟在重放补齐阶段重测(空闲机、冷缓存 sidecar);
  mBERT 为原跑测量。均为含 HTTP 的生产形态。
