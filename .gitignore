# Python
__pycache__/
*.pyc
.venv/
.venv-train/
.pytest_cache/

# Evaluation run outputs (immutable artifacts, never committed)
runs/

# Environment — credentials are env-only, never files
.env

# LongBench data bundle (gitignored; restore via curl, see eval/manifests/subsets.toml source_url)
_downloads/
